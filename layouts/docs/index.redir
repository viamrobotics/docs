# Netlify redirects. See https://www.netlify.com/docs/redirects/
{{ range $p := .Site.AllPages }}
{{- range .Aliases -}}
{{ . }} {{ $p.RelPermalink }}
{{ end }}
{{- if eq $p.Kind "section" -}}
{{ (strings.TrimSuffix "/" $p.RelPermalink) }}.md {{ $p.RelPermalink }}index.md 200
{{ end -}}
{{ end }}
